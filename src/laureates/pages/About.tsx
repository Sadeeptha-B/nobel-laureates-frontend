import React from "react";

type Props = {};

const About = () => {
  return (
    <div className="m-20 text-gray-500 dark:text-gray-600">
      Made by Sadeeptha Bandara. Uses React + Vite + TS + TailwindCSS with
      Flowbite components. Backend made with Express + MongoDB with Mongoose +
      TS. Implements JWT based authentication with Access Tokens and Refresh tokens.
    </div>
  );
};

export default About;
