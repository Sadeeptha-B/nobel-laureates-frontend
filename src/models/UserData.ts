// Model auth related path user data
export interface UserAuthData {
  userId: string;
  email: string;
  token: string;
}

export interface UserDetails {
  userId: string;
  name: string;
  email: string;
}

 
